<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only network policy: allow cleartext HTTP to a developer machine's
  RetailOps backend so a debug build can be pointed at `http://localhost:8000`
  (via `adb reverse tcp:8000 tcp:8000`) or `http://10.0.2.2:8000` (emulator host
  loopback) without an HTTPS certificate.

  This lives in src/debug/ on purpose — release builds keep Android's default
  cleartext-denied policy, so a production station still requires an HTTPS
  backend as documented in CAPACITOR.md.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
